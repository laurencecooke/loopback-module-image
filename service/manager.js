var fs = require('fs');


module.exports = function(app, storagePath) {
  var moduleClass = {
    rename: function (fileId, name) {

    },
    move: function (fileId, path, name) {

    },
  };

  return moduleClass;
};
